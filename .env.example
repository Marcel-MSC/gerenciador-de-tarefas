# Habilita MSW no browser (obrigatório para Docker e dev sem backend)
VITE_ENABLE_MSW=true

# Base path da API mockada
VITE_API_BASE=/api

# Persistência dos dados mockados (false usa memória apenas)
VITE_USE_LOCAL_STORAGE=true
